# 复制为 .env 后修改（deploy.sh 会在缺失时自动从本文件生成）
# MySQL root 密码，app 的 DB_PASSWORD 与之保持一致
MYSQL_ROOT_PASSWORD=changeMe123!

# 初始管理员凭据（首次启动由应用 DatabaseInitializer 创建）
# 留空 ADMIN_PASSWORD 则由应用自动生成随机强密码并打印到日志
ADMIN_USERNAME=admin
ADMIN_PASSWORD=

# 应用日志级别：debug / info / warn
LOG_LEVEL=info
